slam_toolbox:
  ros__parameters:

    # Plugin params
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: DENSE_QR
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: Huber

    # ROS Parameters
    odom_frame: <robot_namespace>odom
    map_frame: <robot_namespace>map
    base_frame: <robot_namespace>base_footprint
    scan_topic: scan
    mode: localization #mapping

    # if you'd like to immediately start continuing a map at a given pose
    # or at the dock, but they are mutually exclusive, if pose is given
    # will use pose
    map_file_name: '/home/app/ros2_ws/src/jetbot_bringup/maps/map_labirint_mini'
    map_start_pose: [0.0, 0.0, 0.0]
    # map_start_at_dock: true
    restamp_tf: false

    debug_logging: false
    throttle_scans: 1
    transform_publish_period: 0.05 #if 0 never publishes odometry
    map_update_interval: 0.5
    resolution: 0.015
    min_laser_range: 0.02 #for rastering images
    max_laser_range: 2.0 #for rastering images
    minimum_time_interval: 0.3
    transform_timeout: 0.5
    tf_buffer_duration: 30.0
    stack_size_to_use: 40000000 #// program needs a larger stack size to serialize large maps
    enable_interactive_mode: true

    # General Parameters
    use_scan_matching: true
    use_scan_barycenter: false
    minimum_travel_distance: 0.02
    minimum_travel_heading: 0.03
    scan_buffer_size: 40
    scan_buffer_maximum_scan_distance: 2.0
    link_match_minimum_response_fine: 0.15  
    link_scan_maximum_distance: 1.0
    loop_search_maximum_distance: 5.5
    do_loop_closing: true 
    loop_match_minimum_chain_size: 20           
    loop_match_maximum_variance_coarse: 1.5 
    loop_match_minimum_response_coarse: 0.5  
    loop_match_minimum_response_fine: 0.7

    # Correlation Parameters - Correlation Parameters
    correlation_search_space_dimension: 0.3
    correlation_search_space_resolution: 0.005
    correlation_search_space_smear_deviation: 0.05 

    # Correlation Parameters - Loop Closure Parameters
    loop_search_space_dimension: 3.0
    loop_search_space_resolution: 0.02
    loop_search_space_smear_deviation: 0.015

    # Scan Matcher Parameters
    distance_variance_penalty: 0.3      
    angle_variance_penalty: 0.6    

    fine_search_angle_offset: 0.00349     
    coarse_search_angle_offset: 0.349   
    coarse_angle_resolution: 0.0349        
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true
    min_pass_through: 2
    occupancy_threshold: 0.1